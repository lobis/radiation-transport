verboseLevel: debug

runManagerType: serial

detector:
  geometry: test.gdml
  volumes:
    - name: sensitiveVolume
      sensitive: true

    - name: notSensitiveVolume

    - name: sensitiveVolumeWithKill
      sensitive: true
      kill: true
